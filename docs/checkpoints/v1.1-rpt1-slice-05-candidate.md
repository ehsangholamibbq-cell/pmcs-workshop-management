# PMCS V1.1 — RPT1 Worker Revocation and Object Integrity Slice 05

- Checkpoint ID: `PMCS-V1.1-RPT1-S05-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `Connected revocation/object-integrity qualification passed | RPT1 exit gates open`
- تاریخ: ۱۴۰۵/۰۶/۲۸ (۲۰۲۶-۰۹-۱۹)
- Parent Slice evidence: `PMCS-V1.1-RPT1-S04-C1` / Run 104
- Candidate source commit: `167133fc1985c5b57c3dac90535f7a962dfd03b7`
- Candidate source tree: `34fb70aee9a62a434a8446444d7c6d5c6c9819bd`
- PR validation merge commit: `f057b82080bf0c6f78fe01f2fd2a33a222aa2400`؛ همان source tree
- Connected validation: Run 108 (`35393509764`) — `success`
- Qualification artifact: `10567196247`
- Artifact digest: `sha256:a62ede8aef0029027a26afd30e88367f637c71c37ca55177bbe6fc487ed2499c`
- Integration artifact: `10566677359`
- Integration artifact digest: `sha256:ebed3efc0e2e32ca9452615437342f5f22d52b95781f252d4d054fb70f713397`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`
- PDF license: `Unconfigured`

## هدف Slice

این Slice معماری حاکم، API خارجی یا Migration را تغییر نمی‌دهد. هدف آن بستن دو پنجرهٔ امنیتی
بعدی و افزودن Evidence صریح برای orphan Generated Document است:

1. ارزیابی دوبارهٔ Permissionهای Reporting و Source بلافاصله پیش از انتشار در Storage؛
2. تعلیق Membership در فاصلهٔ Rendering و publish و شکست fail-closed با
   `reporting.permission.revoked`؛
3. ثبت processing permission snapshot ردشده و lineage صریح Worker؛
4. اثبات نبود Output، Generated Document، release Audit و Outbox پس از revocation؛
5. دستکاری واقعی byteهای شیء MinIO و شکست هم‌زمان Verify و Download؛
6. حذف شیء و آزمون missing object، سپس جایگزینی malformed object و شکست fail-closed؛
7. بازگردانی byteهای اصلی در مسیر `finally` و اثبات Verify معتبر پس از restore؛
8. ثبت Audit مستقل برای هر integrity failure؛
9. شمارش orphan پس از crash-after-storage و اثبات صفرشدن inventory پس از recovery پایدار؛
10. محدودماندن pause و دستکاری object به QA Gateway و محیط ایزوله.

## Evidence متصل قطعی Run 108

- هر هشت Job `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` سبز شد؛
- Build با Warning-as-error و `302/302` تست C# پاس شد؛
- هارنس object security هر `6/6` سناریوی byte-tamper، missing، malformed و restore را پاس کرد و
  orchestration مستقل هر `8/8` assertion را با چهار Audit جدید integrity پاس کرد؛
- worker revocation preparation و verification هر `1/1` و orchestration مستقل هر `8/8` assertion
  را پاس کردند؛ Run با attempt برابر یک، diagnostic صریح و بدون Output/Document/Outbox شکست خورد؛
- processing permission snapshot هر دو تصمیم ردشدهٔ `reporting.run.create` و
  `field.daily-reports.read` را ثبت کرد؛
- recovery regression قبلی با inventory صریح orphan از `15/15` به `17/17` assertion افزایش یافت:
  یک orphan در crash window و صفر orphan پس از reuse سند پایدار؛
- Core Reporting، Cancellation، Security و Worker Recovery قبلی بدون کاهش پوشش پاس ماندند؛
- QA database assertions، backup و Restore Drill با inventory کامل `43` Migration پاس شدند؛
- پنج browser scenario موجود و `139/139` تست Web بدون Regression پاس شدند؛
- Qualification artifact هر هفت Suite را با صفر failure ثبت کرد.

## مرز Testability و عملیات

Pause جدید `BeforeStoragePermissionRecheck` فقط وقتی فعال می‌شود که
`PMCS_QA_GATEWAY_ENABLED=true`، target Run ID، Worker instance ID و مدت محدود ۱ تا ۶۰ ثانیه معتبر
باشند؛ در غیر این صورت startup fail-closed است. دستکاری مستقیم MinIO نیز فقط روی fixture قطعی QA
انجام و byteهای اصلی در `finally` بازگردانده می‌شوند.

Inventory متصل orphan، مشاهده و اثبات recovery را پوشش می‌دهد؛ این نتیجه به معنی وجود sweeper،
حذف خودکار، retention remediation یا مجوز پاک‌سازی Production نیست. طراحی و Qualification آن مسیر
همچنان Gate باز است.

## Gateهایی که این Slice نمی‌بندد

- sweeper/remediation تولیدی و policy عملیاتی orphan Generated Document؛
- retry storm، poison isolation، load/soak، fairness و budgetهای row/page/byte/time؛
- metrics، Worker heartbeat، queue age، alert و recovery inventory عملیاتی؛
- Golden معنایی کامل و Golden مستقل XLSX؛
- PDF واقعی/Golden/performance تا پس از تصمیم حقوقی license؛
- UI اختصاصی Reporting و Gateهای UX2/Accessibility/Responsive آن؛
- Full Regression نهایی و Gate خروج کامل RPT1.

## نقطهٔ دقیق ادامه

1. retry storm و poison-run isolation؛
2. load/soak بیست Run، fairness میان پروژه‌ها و budgetهای row/page/byte/time؛
3. metrics، Worker heartbeat، queue age، alert و recovery/orphan inventory عملیاتی؛
4. تعریف remediation/sweeper امن برای orphan با retention، dry-run و Audit؛
5. Golden معنایی و XLSX؛
6. پس از تصمیم حقوقی license، PDF Golden و performance؛
7. UI گزارش در UX2 و Full Regression نهایی Gate خروج RPT1.

## اثر بر Roadmap و Agent

RPT1 همچنان Active است. COL1، UX2 و INT1 جابه‌جا نشده‌اند و هر هفت Stage Agent مستقل باقی
مانده‌اند. مرز مصوب
`Agent → Permission-aware Tool → Application Service → Business Rules → Database`
بدون تغییر حفظ شده است.

## Gate statement

این سند یک Connected Qualification Slice را ثبت می‌کند، نه بسته‌شدن RPT1. PMCS V1.1 هنوز
`Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست و Baseline
قفل‌شدهٔ PMCS V1 تغییر نکرده است.
