# PMCS V1.1 — RPT1 Worker Concurrency and Crash Recovery Slice 04

- Checkpoint ID: `PMCS-V1.1-RPT1-S04-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `Connected concurrency/recovery qualification passed | RPT1 exit gates open`
- تاریخ: ۱۴۰۵/۰۶/۲۷ (۲۰۲۶-۰۹-۱۸)
- Parent Slice evidence: `PMCS-V1.1-RPT1-S03-C1` / Run 102
- Candidate source commit: `e1ac3263df53a245b1aefb338a015be4854d367b`
- Candidate source tree: `f58881f7e0a77bf89f65b872d4f988bd154a809f`
- PR validation merge commit: `45fd54c4e8b07e8b272c5e5db9a04e3b8d3e8d62`؛ همان source tree
- Connected validation: Run 104 (`35390054888`) — `success`
- Qualification artifact: `10565871708`
- Artifact digest: `sha256:6e87a3f6aa8a91ce58ad411641d7f216c82cabb28cb9d5caaa224872ca1`
- Integration artifact: `10566216072`
- Integration artifact digest: `sha256:0f9f543d826eea18b5d6515575278b30c661482ae360ed743f45a04ac77b966f`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`
- PDF license: `Unconfigured`

## هدف Slice

این Slice API، Domain، Migration یا معماری حاکم را تغییر نمی‌دهد. هدف آن تبدیل Gateهای دو Worker و
Crash Recovery به Evidence متصل و تکرارپذیر است:

1. اجرای دو Process واقعی Worker با identity مستقل؛
2. نگه‌داشتن lock اولین Run توسط Worker A پس از `FOR UPDATE`؛
3. پردازش Run بعدی توسط Worker B و اثبات عملی `SKIP LOCKED`؛
4. توقف سخت Worker A و بازیابی claim تراکنشی rollback‌شده؛
5. بازیابی lease کهنه در وضعیت `BuildingSnapshot`؛
6. crash پیش از Storage و تکمیل مجدد بدون Document/Output ناقص؛
7. crash پس از Storage و پیش از commit، سپس reuse همان Generated Document پایدار؛
8. جلوگیری از Output، Document، Audit و Outbox تکراری؛
9. ثبت Worker instance ID در Audit بدون افشای دادهٔ گزارش؛
10. محدودماندن pauseهای deterministic به QA Gateway ایزوله و default-off.

## Evidence متصل قطعی Run 104

- هر هشت Job `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` سبز شد؛
- Build Warning-as-error و `298/298` تست C# پاس شد؛
- `Reporting recovery preparation` هر `5/5` سناریو و harness نهایی هر `5/5` سناریو را پاس کرد؛
- orchestration مستقل `reporting-worker-recovery-regression` هر `15/15` assertion را پاس کرد؛
- Runهای `concurrency-locked`، `concurrency-skipped`، `crash-before-storage`،
  `crash-after-storage` و `stale-lease` با attemptهای نهایی `1,1,2,2,2` تمام شدند؛
- هر Run دقیقاً یک Output و یک Document governed دارد و crash-after-storage همان Document پایدار را
  reuse کرد؛
- Core Reporting، Cancellation و Security regressionهای قبلی بدون کاهش پوشش پاس ماندند؛
- QA database assertions، backup و Restore Drill با inventory کامل `43` Migration پاس شدند؛
- پنج browser scenario موجود و `139/139` تست Web بدون Regression پاس شدند؛
- Qualification artifact هر هفت Suite را با صفر failure ثبت کرد.

## مرز Testability Hook

Pause pointهای `AfterSnapshotRowLock`، `BeforeStorage` و `AfterStorage` فقط وقتی فعال می‌شوند که
`PMCS_QA_GATEWAY_ENABLED=true` باشد، target Run ID صریح داشته باشند و Worker instance ID معتبر
ارسال شود. در غیر این صورت startup fail-closed است. هیچ endpoint عمومی، feature production یا
مسیر دورزدن Permission به Runtime اضافه نشده است.

## Gateهایی که این Slice نمی‌بندد

- revocation پس از Queue و حین processing Worker؛
- دستکاری object bytes، missing/malformed object و inventory/sweeper برای orphan Generated Document؛
- retry storm، poison isolation، load/soak، fairness و budgetهای row/page/byte/time؛
- metrics، Worker heartbeat، queue age، alert و recovery inventory عملیاتی؛
- Golden معنایی کامل و Golden مستقل XLSX؛
- PDF واقعی/Golden/performance تا پس از تصمیم حقوقی license؛
- UI اختصاصی Reporting و Gateهای UX2/Accessibility/Responsive آن؛
- Full Regression نهایی و Gate خروج کامل RPT1.

## نقطهٔ دقیق ادامه

1. worker-time permission revocation میان Queue، claim، snapshot و publish؛
2. object-byte/missing/malformed tamper و orphan Generated Document inventory؛
3. retry storm، poison isolation، load/soak، fairness و budgetها؛
4. metrics، heartbeat، queue age و alertهای عملیاتی؛
5. Golden معنایی و XLSX؛
6. پس از تصمیم حقوقی license، PDF Golden و performance؛
7. UI گزارش در UX2 و Full Regression نهایی Gate خروج RPT1.

## اثر بر Roadmap و Agent

RPT1 همچنان Active است. COL1، UX2 و INT1 جابه‌جا نشده‌اند و هر هفت Stage Agent مستقل باقی
مانده‌اند. مرز مصوب
`Agent → Permission-aware Tool → Application Service → Business Rules → Database`
بدون تغییر حفظ شده است.

## Gate statement

این سند یک Connected Qualification Slice را ثبت می‌کند، نه بسته‌شدن RPT1. PMCS V1.1 هنوز
`Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست و Baseline
قفل‌شدهٔ PMCS V1 تغییر نکرده است.
