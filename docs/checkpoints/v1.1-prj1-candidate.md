# PMCS V1.1 — PRJ1 Verification Evidence

- Checkpoint ID: `PMCS-V1.1-PRJ1-RC1`
- Stage: `V1.1-PRJ1 — Controlled Project Bootstrap and Duplication`
- وضعیت: `Closed by connected CI evidence`
- تاریخ: ۱۴۰۵/۰۶/۲۷ (۲۰۲۶-۰۹-۱۸)
- Parent evidence: IAM1 source `86f9f4efd086e4e67823a930fcb3ef1b7249b71f` / Run 88
- Integration parent head: `84a0ecab7d44c345d7580eccbe5d9f3df95c32f9`
- Candidate source commit: `e1b5bf6af813af7324065edc1c91eecf2391eccd`
- Candidate source tree: `0539d3c7b337a1785ffd2334c7c1457f92d629c2`
- Connected validation: Run 92 (`35355855215`) — `success`
- Qualification artifact: `10551513960`
- Artifact digest: `sha256:21d0c525bd4813864cb59a5309baf247971929badf28a4b22cb04b48d565bd58`

## خروجی پیاده‌سازی

- Aggregate مستقل `ProjectBootstrapPlan` با State، optimistic Revision، Preview expiry، Digest و Result؛
- Migration شماره ۴۱ با شناسه `projects:20260918-007`؛
- Manifest غیر Legacy برای `projects.core`، چهار Permission مستقل، Navigation و Event نسخه‌دار؛
- یازده Contributor allowlisted و versioned برای Setup، Calendar، Location، Template/Lookup default و Member reference؛
- Preview چهارحالته `Added/Skipped/Conflict/Blocked` با denylist دائمی دادهٔ عملیاتی؛
- Execute مبتنی بر همان Digest، Conflict policy، Idempotency، Audit، Outbox و Correlation؛
- Identity contributor بدون ایجاد User جدید، با کنترل Account/Membership state، اعلان durable و recovery از partial failure؛
- مقصد Draft تا Validation و Activation مستقل با Readiness gate؛
- Result JSON قابل دانلود؛
- Wizard فارسی/RTL در مسیر `/project-bootstraps` با نگهداری محلی انتخاب‌ها در Offline، Preview، Confirmation و Result؛
- تست Domain، Web contract، repository/system audit، migration verification و connected smoke scenario.

## Evidence قطعی Run 92

- هر هشت Job شامل `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`، `identity-container` و `qualification-report` سبز شد؛
- build با `TreatWarningsAsErrors` و `284/284` تست C# پاس شد؛
- `139/139` تست Web، ESLint، ممیزی فارسی، ممیزی تقویم و Next.js production build پاس شد؛
- پنج سناریوی مرورگر واقعی Login/RTL/Responsive/Offline پاس شد؛
- connected create/preview/replay/execute/result/activate، Membership reference-only، Audit و Outbox اثبات شد؛
- assertion پایگاه داده، نبود Daily Report در مقصد و Activation مستقل پاس شد؛
- Repository validation برای ۳۰۵ فایل C# و System contract audit با `265 endpoints / 201 mutations / 5 protocol-managed` پاس شد؛
- PostgreSQL/Object Storage regression، QA reset/seed و Restore Drill با ۴۱ Migration پاس شد.

## ردپای اصلاح Validation

- Run 90 (`35355132763`) شش تخلف analyzer را بدون خطای معماری یا رفتاری آشکار کرد؛ در commit `4091c742d223b31b8f71e16d75b31b70182aa0d5` اصلاح شدند.
- Run 91 (`35355521770`) backend را سبز کرد و ناسازگاری Validator قدیمی با Permission مصوب `projects.bootstrap.members_copy` را هنگام startup آشکار کرد.
- Candidate نهایی همان Permission مصوب را حفظ کرد، Validator را فقط برای شناسه‌های Permission سازگار کرد و تست بازگشتی افزود؛ Run 92 سپس تمام Gateها را سبز کرد.

## نتیجه Gate

Gate خروج PRJ1 بسته است و مرحلهٔ فعال بعدی طبق Roadmap،
`V1.1-RPT1 — Reporting Center Phase 1` است.

این Evidence فقط Checkpoint توسعه PRJ1 را می‌بندد. PMCS V1.1 هنوز `Feature Complete`،
`Qualified`، `Final` یا `Baseline Locked` نیست و PMCS V1 قفل‌شده تغییری نکرده است.
