# PMCS V1.1 — IAM1 Verification Evidence

- Checkpoint ID: `PMCS-V1.1-IAM1-RC1`
- Product status: `V1.1 Development`
- Stage: `V1.1-IAM1 — Configurable Login and Minimal Member Profile`
- Candidate status: `Verified`
- Parent branch: `v1.1-development`
- Parent commit: `862b7fb12a78b6bc180584b12e70e05b8a42fe76`
- Candidate source commit: `86f9f4efd086e4e67823a930fcb3ef1b7249b71f`
- Validation: Run 88 / `35345558791` / `success`
- Date: ۱۴۰۵/۰۶/۲۷ (۲۰۲۶-۰۹-۱۸)

## Verified scope

- یک `MemberProfile` tenant-consistent برای هر حساب و backfill قطعی حساب‌های موجود؛
- Self-service مشخصات حداقلی با revision هم‌زمان User/Profile و Directory scope مستقل؛
- تصویر پروفایل خصوصی با Upload، Quarantine، مالکیت، SHA-256، crop و Release محدود به مالک؛
- `LoginExperienceDescriptor` نسخه‌دار با Draft، Publish، Rollback و fallback داخلی؛
- Composition، رنگ، متن و Motion فقط از allowlist و مستقل از OIDC/BFF/Session؛
- لوگو و Hero امن از Shared Documents با مسیر عمومی version-pinned و immutable؛
- رابط «مدیریت ممتاز» فارسی/RTL با سرمه‌ای، سبز و سطوح گرم، wireframe motion و reduced-motion؛
- پنل مدیریت ظاهر Login، صفحه پروفایل و Navigation مجوزمحور؛
- Permission، Audit، Outbox و Idempotency مرکزی و Eventهای بدون PII/Secret؛
- Migration شماره ۴۰ و Gateهای PostgreSQL/MinIO/Keycloak/Playwright.

## Hard boundaries preserved

- V1 قفل‌شده تغییر وضعیت نداده و این Checkpoint فقط روی خط V1.1 است؛
- Login presentation اجازه تغییر issuer، client، redirect، credential، MFA یا Session را ندارد؛
- HTML/CSS/JavaScript/iframe/remote executable URL قابل بارگذاری نیست؛
- Profile مشترک Tenant است، اما Membership/Role/Scope همچنان برای هر پروژه مستقل است؛
- `member-profile.avatar.publish-self` و `login-experience.manage` مجوز عمومی Release فایل نمی‌دهند؛
- پیام خصوصی، تماس صوتی و تماس تصویری وارد Scope نشده‌اند؛
- Agent مدیریتی هفت‌مرحله‌ای و مرز بدون دسترسی مستقیم Database دست‌نخورده‌اند.

## Defects closed during validation

- مقایسهٔ Header دارایی Login در Smoke test نسبت به whitespace استانداردشده مقاوم شد؛
- Middleware امنیتی فقط Cache immutable دارایی عمومی و version-pinned صفحهٔ ورود را حفظ می‌کند و تمام APIهای دیگر همچنان `no-store` هستند؛
- `PMCS_LOGIN_TENANT_ID` از سرویس Keycloak حذف و فقط به کانتینر Web تزریق شد؛ تست ضد‌رگرسیون Compose نیز افزوده شد.

## Validation evidence

- هر ۸ Job در GitHub Actions Run 88 سبز؛
- Build دات‌نت ۱۰ با صفر Warning و Error و ۲۷۹ تست C# پاس؛
- ۱۳۷ تست Web در CI، ESLint، ممیزی فارسی/شمسی و Production build پاس؛
- ۵ سناریوی مرورگر واقعی شامل OIDC، RTL/Responsive، Motion عادی و کاهش‌یافته، Profile و مدیریت Login پاس؛
- Architecture Gate روی ۲۹۷ فایل C# پاس؛
- System Contract Audit روی ۲۵۹ Endpoint، ۱۹۷ Mutation و ۵ Mutation پروتکلی پاس؛
- PostgreSQL/MinIO واقعی شامل fallback، asset، versioning، rollback، concurrent draft allocation، تصویر پروفایل و privacy-safe events پاس؛
- QA Permission/Workflow/Database/Audit، File/Attachment و Offline/Sync بدون Regression پاس؛
- Restore Drill با ۴۰ Migration پاس؛
- Qualification report artifact: `sha256:3bbe89619ac98b0d33c6507e29f2088ddc6a7b59db0312bdf666ac5a0e4efc1d`.

## Gate statement

Full CI روی Candidate source commit هر هشت Job را سبز کرد و Gate خروج IAM1 بسته شد. این بستن
به‌معنی Feature Complete، Qualified یا Locked شدن V1.1 نیست. مرحلهٔ فعال بعد
`V1.1-PRJ1 — Controlled Project Bootstrap and Duplication` است.

مرجع: [GitHub Actions Run 88](https://github.com/ehsangholamibbq-cell/pmcs-workshop-management/actions/runs/35345558791)
