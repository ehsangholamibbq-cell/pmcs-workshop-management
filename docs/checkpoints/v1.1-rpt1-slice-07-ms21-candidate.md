# PMCS V1.1 — RPT1 Slice 07 Micro-Step 21 F06 Connected Pipeline

- Checkpoint ID: `PMCS-V1.1-RPT1-S07-MS21-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `Connected Safe Checkpoint | F07-F10/UI/Production open | RPT1 active`
- تاریخ: ۱۴۰۵/۰۷/۰۴ (۲۰۲۶-۰۹-۲۶)
- Parent checkpoint: `PMCS-V1.1-RPT1-S07-MS20-C1`
- Parent checkpoint commit: `32772e1f19c9c9d8023947654a3402f53ee0f6b6`
- Parent checkpoint tree: `5eadc44c115ffb6e44cc57b52a44ec0ea5bddc55`
- Candidate source commit: `df3879dd8b17403787154a398cc114b27c7172bc`
- Candidate source tree: `5483e684aaa220a32b3135ea0b2bb3b2136023be`
- PR validation merge commit: `0900def8f237a8d282501a8ee4ae0be5676f2fda`
- PR validation merge tree: `5483e684aaa220a32b3135ea0b2bb3b2136023be`
- Full validation: Run 202 (`36235821024`) — `success`
- Qualification artifact: `10903829279`
- Qualification artifact digest: `sha256:86b5a127f3c04d9732489668e97c58ac2c828d159e94e67bda06cdbf9426d12c`
- Integration artifact: `10904376645`
- Integration artifact digest: `sha256:6a99e3a7ce2b4104085c71e29d9eadb43b6bd9898f821dcb5cb53a3871cd5ae9`
- UI-E2E artifact: `10904401482`
- UI-E2E artifact digest: `sha256:e21ac77c6878a47ce455487c8ccf480cb5dfef099729b5cda85619f5feb1cd41`
- Contract: `PMCS-RPT1-F06-SEMANTIC-001 v1.3.1`
- Runtime identity: `project-commercial-procurement-supply-certified/1.0.0`
- Parameter schema: `pmcs.reporting.project-commercial-procurement-supply.parameters/v1`
- Snapshot schema: `pmcs.reporting.project-commercial-procurement-supply.snapshot/v1`
- Pinned project profile: `pmcs.reporting.project-commercial-procurement-supply.project-profile/v1`
- Template version: `1.0.0`
- Template content digest: `e5966e5910ff9d875151b3e0c5891fb2c36027e8608771d34ee0a5d67120efb5`
- Renderer contract: `pmcs.reporting.project-commercial-procurement-supply.renderer/v1`
- Layout contract: `pmcs.reporting.project-commercial-procurement-supply.layout/v1`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`،
  `OrphanRemediationMode=Disabled`، `PdfLicense=Unconfigured`

## اتصال قطعی F06

این Micro-Step، Runtime Core و Renderer/Golden نسخه‌دار F06 را بدون بازکردن مسیر Production به
pipeline مشترک Reporting متصل کرد:

- Migration forward شمارهٔ 48 با version `20260926-007`، Definition و Template ثابت
  `project-commercial-procurement-supply-certified/1.0.0` را با Classification برابر
  `Confidential`، فرمت‌های `Pdf/Xlsx` و هر شش Permission منبع `commercial-state.read`،
  `commercial.parties.read`، `contracts.read`، `procurement.requests.read`،
  `procurement.orders.read` و `supply.read` در Catalog allowlist ثبت می‌کند؛
- API پارامتر Client را دقیقاً به `{}` محدود می‌کند و هر Contract/Party/Supplier/Request/Order،
  تاریخ محلی، filter، currency، item/category یا Source selector اضافه را با
  `reporting.parameters.invalid` رد می‌کند؛
- Project profile نسخه‌دار، Tenant/Project، Time Zone، base currency، revision/configuration،
  `asOfUtc` و cutoff را هنگام پذیرش Run pin و هنگام Worker دوباره validate می‌کند؛
- Catalog، Create، List/Get Run، Retry، Cancel، Download و Verify و همچنین
  `IReportingReadService` هر شش Permission منبع Definition را fail-closed ارزیابی می‌کنند؛ نبود حتی
  یکی از آن‌ها کل Definition/Run/Output را پنهان یا deny می‌کند و idempotent replay Gate را دور
  نمی‌زند؛
- Worker مجوزهای `reporting.run.create` و شش Commercial/Procurement/Supply read را در مرزهای پردازش
  و رندر دوباره می‌سنجد، فقط `IProjectCommercialProcurementSupplyReportingSource` را با cutoff
  پین‌شده فراخوانی می‌کند، Snapshot را با builder نسخه‌دار می‌سازد و payload را پیش از render از
  نظر identity/schema/hash دوباره کنترل می‌کند؛
- dispatch خروجی فقط از `ProjectCommercialProcurementSupplyReportRendererRegistry` و Rendererهای
  نسخه‌دار PDF/XLSX استفاده می‌کند و Artifact را از مسیر immutable Generated Document منتشر می‌کند؛
- fixture متصل فعلی بدون رکورد رسمی، Contract، Procurement و Supply را `NoData` نگه می‌دارد؛ هیچ
  صفر، F05 join، Inventory/Stock، FX، Forecast، ranking یا AI ساخته نمی‌شود؛
- هارنس متصل Catalog isolation، strict `{}`، deny با حذف Source permission، create/replay/conflict،
  Run filtering، Worker completion و metadata/download/SHA/ETag/security/Verify هر دو فرمت را پوشش
  می‌دهد؛ PDF سه‌صفحه‌ای و XLSX ده-Sheet نیز در همان مسیر واقعی parse می‌شوند.

## رفع Blocker زیرساخت CI

Runهای 198 تا 200 پیش از تست Integration به‌دلیل حذف دسترسی عمومی image پین‌شده MinIO از Quay و
Docker Hub متوقف شدند. راه‌حل نهایی نسخه را عوض نکرد: `deploy/minio/Dockerfile` همان Binary رسمی
`RELEASE.2025-07-23T15-54-02Z` را از دارایی GitHub upstream با SHA-256 ثابت
`eef6581f6509f43ece007a6f2eb4c5e3ce41498c8956e919a7ac7b4b170fa431` داخل image محلی `scratch`
قرار می‌دهد. Compose و Integration فقط همین build محلی را مصرف می‌کنند؛ credential، feature flag،
Production default یا semantics ذخیره‌سازی تغییر نکرده است.

Run 201 سپس کل Runtime و هارنس F06 را اجرا کرد و فقط Query نهایی QA به ستون غیرموجود
`rendering_permission_snapshot` برخورد کرد. اصلاح نهایی Query، مطابق schema و الگوی F02 تا F05،
دو evidence ذخیره‌شدهٔ `request_permission_snapshot` و `processing_permission_snapshot` را کنترل
می‌کند؛ re-authorization رندر همچنان در Worker اجرا و آخرین evidence worker در ستون processing ثبت
می‌شود. این اصلاح Migration یا Domain model تازه‌ای ایجاد نکرد.

## Evidence قطعی Run 202

- هر هشت Job `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` موفق شد؛
- Build با صفر warning/error و `497/497` تست C#، شامل case تازهٔ permission policy و در مجموع
  `39/39` case متمرکز F06، پاس شد؛
- هارنس متصل F06 تمام `15/15` assertion را پاس کرد؛
- `85/85` تست قراردادی Node، `139/139` تست Web و پنج browser scenario واقعی پاس شدند؛
- repository validator روی checkout پاک CI و `406` فایل C# ماژولی پاس شد؛
- system contract audit همان `274` endpoint، `204` mutation و `5` استثنای پروتکلی مستند را تأیید کرد؛
- Database verification، Audit/Outbox/Idempotency، PostgreSQL/Object Storage و Restore Drill کامل
  `48` Migration سبز شدند؛
- Qualification report هر `7/7` Suite و `12/12` Command را با صفر failure روی PR validation merge
  دارای همان Source tree، `qualified` اعلام کرد.

Artifactهای تکمیلی Run 202:

| Suite | Artifact | SHA-256 |
| --- | ---: | --- |
| architecture | `10903513951` | `8f7a91ac5872760f5f8bdbaece2ded1423e935aa70ca0bf21f2d968de9596e85` |
| backend | `10904102110` | `3694017ffe8bc435784182c5106cabf525045c46b69d7cf56795d0b3b8a336cb` |
| pilot-contract | `10904176878` | `c5b9c582481ed20ab073bf275d2cee66e277f34adc056610a417a1b54d899620` |
| web | `10903289311` | `cc89a9f2ed5d3021ae3821cf4151ba0ca0c0ec31361fa5b4d9ce115a18fea557` |
| identity-container | `10903908471` | `7043f0b466398a9292b01b4953b071c0d630a31be0aa344b0aed261d947a5b4c` |

## مرز این Micro-Step

- UI اختصاصی Reporting و هر تغییر UX2 خارج از Scope باقی مانده است؛
- هیچ feature flag یا Production default روشن نشده و `PdfLicense` همچنان `Unconfigured` است؛
- Migration فقط forward-only Catalog/Template seed است؛ دادهٔ V1 یا بخش‌های checkpointed قبلی
  بازتفسیر، reset یا بازنویسی نشده‌اند؛
- هیچ Report Designer، SQL/query آزاد، subscription یا Production rollout اضافه نشده است؛
- compatibility source جای producer تاریخی کامل را نمی‌گیرد و برای history غیرقابل‌اثبات
  fail-closed باقی می‌ماند؛
- F01 تا F06 اکنون Safe Checkpoint متصل دارند؛ F07 تا F10 هنوز
  `Required / Not Implemented` هستند؛
- Evidence این Checkpoint مجوز Production rollout نیست و RPT1 یا V1.1 هنوز `Feature Complete`،
  `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست.

## نقطه دقیق Resume

Safe Resume Point اکنون `PMCS-V1.1-RPT1-S07-MS21-C1` است. Micro-Step بعدی باید فقط با DoR و
قرارداد معنایی مستقل خانواده `RPT1-F07` برای دفتر فنی شامل Document/RFI/Submittal/Transmittal آغاز
شود. UI/UX2، Production enablement، روشن‌کردن defaultها، Report Designer و Runtime/Renderer/wiring
F07 در همان Micro-Step خارج از Scope باقی می‌مانند.

## Gate statement

Catalog/API/Worker و qualification انتهابه‌انتهای خانواده F06 بسته و قابل ادامه است. F07 تا F10 و
RPT1 باز هستند. PMCS V1.1 هنوز `Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا
`Baseline Locked` نیست و Baseline قفل‌شده PMCS V1 تغییر نکرده است.
