# PMCS V1.1 — RPT1 Generated Output Slice 02 Source Candidate

- Checkpoint ID: `PMCS-V1.1-RPT1-S02-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `Connected CI candidate passed | RPT1 exit gates open`
- تاریخ: ۱۴۰۵/۰۶/۲۷ (۲۰۲۶-۰۹-۱۸)
- Start commit: `6eda96567c7fad91f5db3c90890126a7c5b3364d`
- Candidate source commit: `ef5d68e5d35b7f2b58ebd3da87b3b35dadf19173`
- Candidate source tree: `4deccade8899a2438485fb1304cd918115af2654`
- PR validation merge commit: `ebbe1090f42cf6bd58928d8844a4b0f86e6abcf6`؛ همان source tree
- Connected validation: Run 99 (`35381177208`) — `success`
- Qualification artifact: `10562821142`
- Artifact digest: `sha256:80ae2c86a129b9f3bb9c5b0a7e2078670f823acd6d201e92e8ba224eeea75036`
- Parent Slice: `PMCS-V1.1-RPT1-S01-C1`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`

## خروجی Slice 02

1. Contract باریک `IGeneratedDocumentPublisher` در مالک Documents اضافه شد؛ Reporting object key،
   presign یا upload session عمومی دریافت نمی‌کند؛
2. Generated output با stable Document ID، path server-generated، allowlist media/extension، signature،
   size، SHA-256، exact-byte read-after-write، Release سیستمی، `LongTerm` retention، Audit و
   `documents.asset.released.v1` منتشر می‌شود؛
3. Shared Document reference برای verification فقط metadata لازم Retention/LegalHold را صادر می‌کند
   و generic Documents access برای `ReportOutput` همچنان fail-closed است؛
4. Adapterهای مستقل PDF و XLSX ثبت شدند. PDF دارای A4/Portrait، RTL، فونت فارسی صریح، metadata
   pin‌شده، header/footer/page number و data-status state است. XLSX یک OpenXML deterministic با
   metadata/data sheet، RTL، freeze/filter، numeric cells و formula-injection protection می‌سازد؛
5. stable Output/Document identity، manifest hash، verification code، نام فایل شمسی و تبدیل
   Jalali/Tehran پیاده شد؛
6. Worker از `SnapshotReady` به `Rendering → Succeeded` ادامه می‌یابد، تمام formatها را پیش از هر
   publish رندر/اعتبارسنجی می‌کند، stable retry/crash recovery دارد و final Output/Run/Audit/Outbox
   را transactionally ثبت می‌کند؛
7. Retry محدود و صریح، Cancel پیش از Rendering، Download و Verify permission-aware، current Source
   permission recheck، ETag/no-store/nosniff و integrity-failure Audit اضافه شد؛
8. `OutputAccessEnabled` از Create/Catalog جدا شد تا rollback بتواند تولید جدید را ببندد ولی در نبود
   Incident امنیتی، خروجی‌های موجود را قابل Download/Verify نگه دارد؛
9. Migration شمارهٔ 43 unique verification-code constraint را به index غیر unique تبدیل کرد؛ چون
   دو Manifest معنایی برابر می‌توانند verification code برابر و Output identity جدا داشته باشند؛
10. TestHarness سناریوی Catalog → Create/Replay/Conflict → Worker → XLSX → Documents → Download/
    Verify را به‌همراه Observer/Cross-project denial و PDF license fail-closed + Retry آماده کرد؛
11. QA دیتابیس stateهای Run/Snapshot/Output، Released Document، Retention، Migration 43، Audit،
    Outbox و Idempotency را assert می‌کند؛
12. read-only Reporting application service نیز پشت runtime gates قرار گرفت تا Toolهای Agent آینده
    هنگام خاموش‌بودن feature داده‌ای برنگردانند.

## Evidence محلی اجراشده

| کنترل | نتیجه |
| --- | --- |
| RPT1 source-contract tests | `9/9 passed` |
| تمام Node tool tests | `40/40 passed` |
| تمام Web unit tests | `139/139 passed` |
| Web lint | `passed`؛ صفر warning |
| Persian UI audit | `passed` |
| Persian calendar audit | `passed` |
| Web production build | `passed`؛ Next.js/TypeScript/static generation |
| Repository architecture validation | `passed`؛ ۳۳۴ فایل C# ماژولی |
| System contract audit | `passed`؛ ۲۷۴ endpoint، ۲۰۴ mutation، ۵ protocol-managed mutation |
| Shell syntax | `seed-diagnostics.sh` و `verify-database.sh` پاس |
| Git whitespace validation | `git diff --check` پاس |

Evidence محلی بالا روی tree دقیق Candidate اجرا شد. Run 99 همان tree را در PR merge موقت GitHub
آزمایش کرد؛ بنابراین تفاوت SHA میان head و merge به معنی تفاوت Source نیست.

## Evidence متصل قطعی Run 99

- هر هشت Job شامل `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` سبز شد؛
- Restore/Build با Warning-as-error و `295/295` تست C# پاس شد؛
- `139/139` تست Web، lint، ممیزی فارسی/تقویم و production build پاس شد؛
- پنج سناریوی مرورگر واقعی موجود بدون Regression پاس شدند؛ این نتیجه به معنی وجود UI تولیدی
  Reporting نیست؛
- هارنس Reporting هر `13/13` assertion را روی PostgreSQL و Object Storage واقعی پاس کرد: Catalog،
  Observer deny، Create، replay همان Idempotency-Key، conflict payload متفاوت، Worker/XLSX،
  Download/Verify، cross-project denial، PDF license fail-closed و Retry محدود؛
- Migrationهای 42/43 اعمال شدند، QA database assertions پاس شدند و Restore Drill با inventory کامل
  `43` Migration موفق شد؛
- aggregator هر هفت Suite را با صفر failure در artifact بالا `qualified` اعلام کرد. این واژه فقط
  نتیجهٔ قرارداد CI همان Candidate است و RPT1 یا PMCS V1.1 را Product Qualified اعلام نمی‌کند.

Runs 94 تا 98 نقش diagnostic داشتند: analyzerهای C#، ناسازگاری API نسخهٔ pin‌شده QuestPDF، analyzer
xUnit و در نهایت وابسته‌شدن hash idempotency به cutoff تولیدشدهٔ سرور را آشکار کردند. Candidate
نهایی hash را از payload canonical مشتری می‌سازد و cutoff واقعی را جداگانه pin می‌کند؛ Run 99
replay یکسان و conflict واقعی را هر دو اثبات کرد.

## Gateهای RPT1 که هنوز بازند

- concurrency دو Worker، lease recovery و crash-before/after-storage windows؛
- Permission revocation میان Queue/processing/download، cross-tenant و tamper/malformed-object
  coverage کامل؛
- Cancel متصل، retry storm، load/soak، fairness و row/page/byte/time budgets؛
- PDF واقعی با tier حقوقی تأییدشده، parse/font embedding، byte determinism، Golden/pixel و
  dataset چندصفحه‌ای؛
- Golden معنایی کامل برای cutoff پیش/پس از correction و Golden مستقل XLSX؛
- metrics/heartbeat، orphan Generated Document recovery و alert evidence؛
- UI تولیدی Reporting و Gateهای UX2/RTL/Responsive/Accessibility مربوط به آن.

## تصمیم باز PDF که نباید حدس زده شود

Dependency روی `QuestPDF 2026.8.0` pin شده، اما `ReportingCenter:PdfLicense` عمداً
`Unconfigured` است. انتخاب `Community`، `Professional` یا `Enterprise` یک تصمیم حقوقی/تجاری
سازمان است و در این Slice فرض نشده است. QA فعلی فقط اثبات می‌کند وضعیت تنظیم‌نشده fail-closed است.
تا انتخاب و ثبت tier، اجرای PDF Golden و enable کردن PDF مجاز نیست. نسخه/digest image و فونت
DejaVu نیز باید همراه Golden Candidate ثبت شوند.

## Known limitations صریح

- automated orphan Generated Document sweeper و metrics/heartbeat عملیاتی هنوز پیاده نشده‌اند؛
- QR تصویری تولید نمی‌شود؛ verification code و endpoint امن موجود است و شرط Roadmap
  `QR/Verification Code` را بدون public bypass دنبال می‌کند؛
- Template نخست فقط A4/Portrait است؛ A3/Landscape نیازمند Template version و Golden مستقل است؛
- UI تولیدی Reporting به UX2 و Visual Gateهای باز وابسته است؛
- determinism PDF تا اجرای byte/golden test در image ثابت ادعا نمی‌شود.

## نقطهٔ دقیق ادامه

1. افزودن Qualification متصل crash/concurrency/revocation/tamper و Cancel بدون تغییر معماری؛
2. تکمیل metrics/heartbeat و recovery inventory برای Worker/Generated Document؛
3. ساخت Golden معنایی و XLSX deterministic مستقل؛
4. پس از تصمیم صریح license، PDF Golden/visual/determinism و performance؛
5. پیاده‌سازی UI گزارش در UX2 و اجرای Gateهای مرورگر مربوط به Reporting؛
6. Full Regression نهایی و Evidence مستقل Gate خروج RPT1.

## اثر بر Roadmap و Agent

این Slice هیچ Scope یا تصمیم قبلی را حذف نکرده است. RPT1 همچنان Active است؛ COL1، UX2 و INT1 در
ترتیب مصوب باقی مانده‌اند. Tool manifestهای read-only فقط پایهٔ Stage 1 Agent هستند و هیچ‌یک از
هفت Stage Agent را خودکار کامل یا ادغام نمی‌کنند. مسیر ثابت
`Agent → Permission-aware Tool → Application Service → Business Rules → Database` حفظ شده است.

## Gate statement

این سند Connected Candidate دوم RPT1 را ثبت می‌کند، نه Checkpoint بسته یا Baseline تأییدشده. PMCS
V1.1 هنوز `Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست.
Baseline قفل‌شدهٔ PMCS V1 تغییر نکرده است.
