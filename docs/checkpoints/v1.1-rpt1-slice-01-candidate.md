# PMCS V1.1 — RPT1 Core Slice 01 Source Candidate

- Checkpoint ID: `PMCS-V1.1-RPT1-S01-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `Source Candidate | Local structural evidence only | Unqualified`
- تاریخ: ۱۴۰۵/۰۶/۲۷ (۲۰۲۶-۰۹-۱۸)
- Start commit: `86f97d811217e2b508ba3c2f717506bb5e63bc2a`
- Candidate source commit: `2208bb1828dea571b81a3eb615ddd13250520c73`
- Candidate source tree: `257d8c80f45435e462563e38bb3c5fa12c77808b`
- Parent DoR: `PMCS-V1.1-RPT1-DOR1`
- Feature flag: `ReportingCenter:Phase1Enabled = false`

## خروجی Slice 01

1. Bounded Context مستقل `Pmcs.Modules.Reporting` و Descriptor غیر Legacy با شناسه
   `reporting.center` اضافه شد؛
2. چهار Permission مصوب، Navigation وابسته به `reporting.phase1`، سه Tool Manifest خواندنی برای
   Agent Stage 1 و Event contract نسخه‌دار ثبت شد؛
3. Migration شماره ۴۲ با مالک `reporting` و order برابر ۱۲۰۰، پنج جدول مالکیتی و Seed ثابت
   `daily-report-certified@1.0.0` را اضافه کرد؛
4. `IDailyReportReportingSource` در مالک FieldOperations زنجیرهٔ Approved/Superseded، lineage،
   Fact و cutoff را بدون افشای Persistence صادر می‌کند؛
5. Aggregateهای Run، Snapshot و Output، canonical JSON/SHA-256، lifecycle، Retry داخلی، Cancel،
   Archive و کنترل media/extension پیاده شد؛
6. Catalog/Create/Get/List با Tenant/Project predicate، Permission + Source permission، Project
   Active، Idempotency، Audit، Outbox، Correlation و Permission snapshot پیاده شد؛
7. Worker با `FOR UPDATE SKIP LOCKED`، lease/retry محدود، ارزیابی مجدد Permission و Project،
   Semantic Snapshot قطعی و وضعیت صریح `Available/NoData/InsufficientData` اضافه شد؛
8. Application contract خواندنی `IReportingReadService` برای Catalog، Run status و Output metadata
   اضافه شد تا Agent آینده فقط از مسیر
   `Permission-aware Tool → Application Service → Business Rules → Database` عبور کند؛
9. مسیرهای عمومی Shared Documents برای owner type برابر `ReportOutput` در List/Get/Upload/Release/
   Classification/Download به‌صورت fail-closed مسدود شد؛
10. Role mapping اولیه مطابق Security contract افزوده شد؛ Observer حق Create ندارد و دسترسی Source
    همچنان مستقل و لازم است.

## Evidence محلی قطعی

| کنترل | نتیجه |
| --- | --- |
| Node contract tests | `37/37 passed` |
| Repository architecture validation | `passed`؛ ۳۲۴ فایل C# ماژولی بررسی شد |
| System contract audit | `passed`؛ ۲۷۰ endpoint، ۲۰۲ mutation و ۵ mutation پروتکل‌دار |
| Shell syntax | `reset-database.sh` و `verify-database.sh` پاس |
| Git whitespace validation | `git diff --check` پاس |
| Migration inventory source check | ۴۲ فایل Migration؛ Reporting schema در reset و verification ثبت شد |

## Evidence اجرا نشده و Gateهای باز

در محیط محلی این Candidate، `dotnet`، Docker/PostgreSQL/MinIO و credential مخزن Remote در دسترس
نبود. بنابراین موارد زیر **پاس‌شده اعلام نمی‌شوند**:

- Restore/Build و هشت تست C# جدید Reporting؛
- اجرای واقعی Migration 42 روی PostgreSQL و Restore Drill دارای ۴۲ Migration؛
- connected permission/idempotency/worker concurrency regression؛
- CI هشت‌Job و Qualification artifact روی SHA دقیق Candidate؛
- object-storage، PDF/XLSX، download/verify و Golden/visual evidence.

وجود تست‌های C# در Source به معنی اجرای آن‌ها نیست. Feature flag و Worker به‌طور پیش‌فرض خاموش
هستند تا Pipeline فاقد Renderer در دسترس کاربر قرار نگیرد.

## محدودهٔ باقی‌مانده RPT1

Slice بعدی دقیقاً از این نقطه آغاز می‌شود:

1. Generated Document publish/read contract در مالک Documents؛
2. Rendererهای deterministic PDF و XLSX و policyهای RTL/Jalali/Persian digits؛
3. ادامه Worker از `SnapshotReady` به `Rendering → Succeeded` با publish اتمیک و retry-safe؛
4. Download/Verify، سپس Retry/Cancel API؛
5. Golden semantic/PDF/XLSX، PostgreSQL/Object Storage، Permission revocation، load/soak و restore؛
6. UI تولیدی فقط طبق UX2 و Gateهای باز Visual Excellence؛
7. Full Regression و Evidence متصل برای بستن RPT1.

## Gate statement

این Checkpoint فقط Source Candidate اولین Slice را ثبت می‌کند. RPT1 هنوز بسته نشده و PMCS V1.1
همچنان `Feature Complete`، `Qualified`، `Final` یا `Baseline Locked` نیست. Baseline قفل‌شدهٔ PMCS V1
تغییر نکرده است.
