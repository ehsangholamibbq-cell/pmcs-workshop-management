# PMCS V1.1 — RPT1 Slice 06 Micro-Step 05 Semantic and XLSX Golden

- Checkpoint ID: `PMCS-V1.1-RPT1-S06-MS05-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `MS05 complete | connected semantic cutoff and deterministic XLSX Golden passed | RPT1 active`
- تاریخ: ۱۴۰۵/۰۶/۲۸ (۲۰۲۶-۰۹-۱۹)
- Parent checkpoint: `PMCS-V1.1-RPT1-S06-MS04-C1` / checkpoint validation Run 124
- Initial MS05 source commit: `bc3cc75d41e4f3856e12aa1da68ee7cee7d4523f`
- Final candidate source commit: `38a03f33f4747d0b6a76696705877633acd17678`
- Candidate source tree: `eb9369c9e32eb3f523c4faa22487d6428c2d7e34`
- PR validation merge commit: `65e1e6988430569eca99ba3ecbea01172dd29b94`؛ همان source tree
- Diagnostic validations: Runهای 125 تا 129؛ compile/analyzer و fixture diagnostics
- Full validation: Run 130 (`35449387794`) — `success`
- Qualification artifact: `10586093992`
- Qualification artifact digest: `sha256:c7bbe07dc3e052772665fdffaedce99e85e7c340058eb6bbd3b8590dc0128666`
- Integration artifact: `10585992542`
- Integration artifact digest: `sha256:9ff365ccb881ee47f0afe68b8f46fa521a2b06c24ab52647ce47d1fc714c176f`
- UI-E2E artifact: `10585872681`
- UI-E2E artifact digest: `sha256:74dcf75bd74e0c4389a65c737cac8b312592ea5b99e8ccaf804f228446ecd1ee`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`،
  `OrphanRemediationMode=Disabled`
- PDF license: `Unconfigured`

## مرز این Checkpoint

این Candidate ادامهٔ مستقیم MS04 است و API تجاری، Migration، ترتیب Roadmap یا مرز Bounded Contextها
را تغییر نمی‌دهد. MS05 یک Golden متصل و مستقل برای semantic snapshot و XLSX گزارش روزانه رسمی
اضافه می‌کند:

1. زنجیرهٔ قطعی سه‌نسخه‌ای با v1 رسمی، v2 اصلاح رسمی و v3 اصلاح Draft؛
2. هر هشت نوع Fact، قلم اندازه‌گیری واقعی، quantity/unit، اثر Critical و رشتهٔ formula-like؛
3. حذف Fact مصالح کپی‌شده در v2، replacement رسمی و حفظ lineage هفت Fact کپی‌شده؛
4. چهار Run قطعی: دو twin پیش از correction و دو twin پس از correction؛
5. برابری Snapshot/Source Manifest در هر twin و تغییر هر دو hash پس از correction رسمی؛
6. حذف کامل v3 Draft و marker آن از هر Snapshot و workbook؛
7. replay idempotent و دانلود دوبارهٔ همان Output با bytes و SHA-256 یکسان؛
8. parser مستقل ZIP/OpenXML برای entryها، ترتیب sheet، RTL، freeze pane، filter، header، metadata،
   نبود formula/macro/external link و تطبیق تمام ۱۸ ستون؛
9. assertion مستقیم PostgreSQL برای chain، Factها، چهار Run/Output، revisionهای تاریخی و draft exclusion.

## اصلاح semantic تاریخی

خواندن `asOf` پیش از correction اکنون state، link، timestamp، correction reason و revision مربوط به
supersession آینده را پنهان می‌کند و `LastModifiedAt` را به زمان Approval همان نسخه برمی‌گرداند.
در cutoff نخست فقط v1 با state `Approved` و revision `11` دیده می‌شود؛ در cutoff دوم v1 با state
`Superseded` و revision `12` کنار v2 رسمی با revision `5` قرار می‌گیرد. یک Unit test مستقیم نیز این
projection تاریخی را بدون وابستگی به renderer قفل می‌کند.

## Evidence قطعی Run 130

- هر هشت Job `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` سبز شد؛
- Build با Warning-as-error و `329/329` تست C# پاس شد؛
- `51/51` تست قراردادی Node، validator روی `343` فایل C# ماژولی و system contract audit با
  `274` endpoint، `204` mutation و `5` استثنای مستند پاس شدند؛
- Golden متصل هر `13/13` assertion را پاس کرد؛ v1/v2 هر کدام هشت Fact داشتند، هفت Fact v2 lineage
  کپی داشتند و v3 با نه Fact فقط Draft باقی ماند؛
- Snapshotهای پیش و پس به‌ترتیب hashهای
  `8d2f6f13463d2209b51c9d24a841bbd5a58d8523511465b9c00e7242ae1c0d1f` و
  `b2d4fc67525c49a6772564317e73322f5cd240e84d4c437ce902ec2125cecc9c` را ثبت کردند؛
- workbookهای پیش و پس به‌ترتیب ۸ و ۱۶ ردیف داده و semantic digestهای
  `1514c7056eba23d9238449ed0debd3a5d605eef41a612cd1295de7031d752e70` و
  `0628d81966941926ad038146af08468f21d04d900ee1e80ac6402c200ddf4fdf` داشتند؛ twinهای هر cutoff
  digest یکسان داشتند و دو cutoff متفاوت ماندند؛
- دانلود تکراری Output پیش و پس با SHA-256های
  `e59a42c2f4d6319e74d880aeb19c9c07d44bccc416238629b5cff09eb2d8a51a` و
  `f12f573e0dbd059a6b284c4e988ef425f937ca78285947bedcc0cd8421d7648a` byte-identical ماند؛
- `139/139` تست Web، پنج browser scenario، تمام regressionهای متصل قبلی و Restore Drill کامل
  `43` Migration پاس شدند؛ Qualification report هر هفت Suite را با صفر failure ثبت کرد.

## Diagnostic Runها و Fixهای محدود

- Run 125 برخورد نام helper `Sha256` را با `CS0111` آشکار کرد؛ helper Golden نام مستقل گرفت؛
- Run 126 analyzer دات‌نت ۱۰، پارامتر concrete را با `CA1859` الزام کرد؛ امضای helper دقیق شد؛
- Run 127 خواندن `null` معتبر فیلدهای عددی اختیاری را آشکار کرد؛ parser نوع JSON را پیش از parse
  کنترل کرد؛
- Run 128 تفاوت دقت ۱۰۰ نانوثانیه‌ای پاسخ اولیه و دقت میکروثانیه‌ای PostgreSQL را آشکار کرد؛
  workbook با cutoff مرجع و persist‌شدهٔ خود Run سنجیده شد و diagnostics OpenXML نام‌دار شد؛
- Run 129 تداخل تاریخ fixture Golden با fixture قدیمی Sync را آشکار کرد؛ تاریخ Golden به اسلات
  مستقل `2099-12-27` منتقل شد، بدون تغییر رفتار Sync یا Production.

## چیزی که این Checkpoint ادعا نمی‌کند

- این Evidence، license قانونی PDF را تعیین یا QuestPDF را برای Production فعال نمی‌کند؛
- PDF واقعی، visual/pixel Golden، فونت/renderer pin و performance budget هنوز پاس نشده‌اند؛
- XLSX تنها قالب استاندارد `daily-report-certified/1.0.0` را پوشش می‌دهد و Report Designer آزاد یا
  Query/SQL کاربر را مجاز نمی‌کند؛
- UI اختصاصی Reporting و visual regression آن در UX2 هنوز Gate باز است؛
- RPT1 بسته نیست و این نتیجه مجوز روشن‌کردن هیچ feature flag در Production نیست.

## نقطه دقیق Resume

Golden معنایی و XLSX با MS05 بسته شدند. گام بعدی فقط پس از تصمیم حقوقی صریح دربارهٔ license، ثبت
نسخه/digest renderer image و فونت و اجرای PDF Golden/visual/performance ادامه می‌یابد. UI گزارش نیز
طبق Roadmap در UX2 باقی می‌ماند؛ هیچ Scope یا مرحلهٔ تازه‌ای جایگزین این ترتیب نشده است.

## اثر بر Roadmap و Agent

RPT1 همچنان Active است. COL1، UX2 و INT1 جابه‌جا نشده‌اند و هر هفت Stage Agent مستقل باقی
مانده‌اند. مرز مصوب
`Agent → Permission-aware Tool → Application Service → Business Rules → Database`
بدون تغییر حفظ شده است.

## Gate statement

این سند Safe Checkpoint `S06-MS05` است، نه بسته‌شدن RPT1 یا V1.1. PMCS V1.1 هنوز
`Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست و Baseline
قفل‌شدهٔ PMCS V1 تغییر نکرده است.
