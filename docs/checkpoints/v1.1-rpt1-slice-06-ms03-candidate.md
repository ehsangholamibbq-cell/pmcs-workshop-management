# PMCS V1.1 — RPT1 Slice 06 Micro-Step 03 Operational Signal Contract

- Checkpoint ID: `PMCS-V1.1-RPT1-S06-MS03-C1`
- Stage: `V1.1-RPT1 — Reporting Center Phase 1`
- وضعیت: `MS03 in progress | bounded signal contract and connected queue-age health passed | exporter/scrape/alert delivery pending`
- تاریخ: ۱۴۰۵/۰۶/۲۸ (۲۰۲۶-۰۹-۱۹)
- Parent checkpoint: `PMCS-V1.1-RPT1-S06-MS02-C1` / checkpoint validation Run 114
- Initial MS03 source commit: `6f2499e1a8280b074b2fb7771bf2878d84086c58`
- Final candidate source commit: `83f13cf43679b23a6a169cc0912985b391b1c017`
- Candidate source tree: `1705d184bd494e80e50d8a85b723f0bc63e20abc`
- PR validation merge commit: `f1c085806e0e55e298e0680e3f776ee19133265e`؛ همان source tree
- Diagnostic validation: Runs 115 و 116 — به‌ترتیب analyzer و متغیر shell هارنس
- Full validation: Run 117 (`35441980440`) — `success`
- Qualification artifact: `10584605085`
- Qualification artifact digest: `sha256:a370b711917075776241755b7be0e6c4f96e6069b4e87fade2a3bf0dc0e21f8d`
- Integration artifact: `10583793574`
- Integration artifact digest: `sha256:ba05ca52ddf8acecef179d64a36b649200494d229cf59230bea75a4e80a1117b`
- UI-E2E artifact: `10584530123`
- UI-E2E artifact digest: `sha256:46fecb83d5d87eab4135b0ce9be25b8578ea611449cae094220c0cafee0324eb`
- Feature defaults: `Phase1Enabled=false`، `OutputAccessEnabled=false`، `WorkerEnabled=false`
- PDF license: `Unconfigured`

## مرز این Checkpoint

این Candidate نخستین Safe Checkpoint داخل MS03 است و Scope یا ترتیب Roadmap را تغییر نمی‌دهد.
هیچ endpoint تجاری زیر `/api/v1` و هیچ Migration تغییر نکرده است. تغییر عملیاتی محدود است به:

1. انتشار فقط چهار مقدار عددی allowlist‌شدهٔ `activeRuns`، `heartbeatAgeSeconds`،
   `oldestQueueAgeSeconds` و `queuedRuns` برای check دقیق `reporting-worker` در پاسخ readiness؛
2. حذف fail-closed تمام dataهای سایر health checkها و تمام مقدارهای غیرعددی یا خارج allowlist؛
3. Qualification واحد برای نه instrument پایدار Meter `Pmcs.Reporting` و فقط چهار tag کم‌کاردینالیتی
   `reporting.work_kind`، `reporting.retry`، `reporting.outcome` و `reporting.diagnostic_code`؛
4. گسترش سناریوی connected fairness تا صف aged را زیر دو row lock واقعی نگه دارد و وضعیت
   `Degraded` ناشی از queue age را همراه payload عددی محدود اثبات کند؛
5. کنترل عدم نشت Tenant، Project، User یا Run ID در readiness payload.

Readiness یک سطح تشخیصی عملیاتی است، نه مسیر Authorization یا API دادهٔ گزارش. description فقط
وضعیت budget را بیان می‌کند و هیچ Snapshot، diagnostic detail، object key یا identity منتشر نمی‌کند.

## Diagnostic Runs و Fixهای محدود

Run 115 پیش از اجرای تست‌ها روی قانون analyzer `CA1861` متوقف شد. آرایهٔ ثابت expectation به
`static readonly` منتقل شد و Runtime تغییر نکرد. Run 116 سپس Build و هر `313/313` تست C# را پاس
کرد و log متصل نیز `Degraded` صحیح با پیام queue-age را ثبت کرد؛ failure فقط از متغیر تعریف‌نشدهٔ
shell در فهرست forbidden identity بود. Fix نهایی همان Tenant/User منبع و Project/Runهای fixture را
به‌طور صریح bind کرد و کنترل عدم نشت را حذف یا ضعیف نکرد.

## Evidence قطعی Run 117

- هر هشت Job `architecture`، `backend`، `integration`، `pilot-contract`، `web`، `ui-e2e`،
  `identity-container` و `qualification-report` سبز شد؛
- Build با Warning-as-error و `313/313` تست C# پاس شد؛
- `46/46` تست قراردادی Node، Repository validator روی `338` فایل C# ماژولی و system contract
  audit با `274` endpoint، `204` mutation و `5` استثنای مستند پاس شدند؛
- Capacity regression هر `11/11` assertion، ۲۰ Run سالم، poison سه-attemptی و P95 برابر
  `5.871168s` در budget سی‌ثانیه‌ای را پاس کرد؛
- Fairness regression هر `10/10` assertion را با دو پروژه، دو Worker و marker قطعی
  `health=degraded-queue-age` پاس کرد؛
- health check در صف aged وضعیت `Degraded`، شرح queue-age و چهار مقدار عددی محدود را برگرداند و
  هیچ Tenant/Project/User/Run ID در payload نبود؛
- `139/139` تست Web و پنج سناریوی مرورگر واقعی UI-E2E پاس ماندند؛
- PostgreSQL، MinIO و Restore Drill کامل `43` Migration پاس شد؛
- Qualification report هر هفت Suite را با صفر failure ثبت کرد.

## چیزی که این Checkpoint هنوز ادعا نمی‌کند

- MS03 کامل نشده است: exporter، scrape endpoint/collector، alert rule و تحویل واقعی alert هنوز
  پیاده‌سازی و Qualification نشده‌اند؛
- وجود Meter و health payload به‌تنهایی Production Observability نیست؛
- orphan inventory قبلی، sweeper/remediation تولیدی نیست؛
- Golden معنایی و XLSX کامل، تصمیم حقوقی license و Golden/Performance PDF و UI اختصاصی Reporting
  هنوز Gate باز هستند؛
- این نتیجه مجوز روشن‌کردن Feature flagها در Production نیست.

## نقطه دقیق Resume

ادامهٔ همین Micro-Step با `PMCS-V1.1-RPT1-S06-MS03-C2` است: exporter و scrape deployment-facing،
alert ruleهای queue age/heartbeat/failure-retry و evidence تحویل alert، بدون افزودن identityهای
پرکاردینالیتی. پس از بسته‌شدن MS03، remediation امن orphan، Golden معنایی/XLSX، PDF پس از تصمیم
حقوقی و UI گزارش در UX2 طبق همان Roadmap ادامه می‌یابند.

## اثر بر Roadmap و Agent

RPT1 همچنان Active است. COL1، UX2 و INT1 جابه‌جا نشده‌اند و هر هفت Stage Agent مستقل باقی
مانده‌اند. مرز مصوب
`Agent → Permission-aware Tool → Application Service → Business Rules → Database`
بدون تغییر حفظ شده است.

## Gate statement

این سند Safe Checkpoint میانی `S06-MS03-C1` است، نه بسته‌شدن MS03 یا RPT1. PMCS V1.1 هنوز
`Feature Complete`، `Release Candidate`، `Qualified`، `Final` یا `Baseline Locked` نیست و Baseline
قفل‌شدهٔ PMCS V1 تغییر نکرده است.
